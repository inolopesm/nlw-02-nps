<style>
    .container {
        width: 800px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        align-content: center;
    }

    .value {
        padding: 10px;
        margin: 5px;
        background: #8257E6;
        color: #FFF;
        border-radius: 6px;
        width: 15px;
        text-align: center;
        text-decoration: none;
    }

    .level {
        width: 350px;
        margin: 10px;
        display: flex;
        justify-content: space-between;
    }

    .answers {
        width: 350px;
        display: flex;
        justify-content: space-between;
    }
</style>
<div class="container">
    <label>Olá <strong>{{name}}</strong>! Tudo bem?</label>

    <h3>{{title}}</h3>
    <br />
    <strong>{{description}}</strong>

    <div class="level">
        <span>Pouco provável</span>
        <span>Muito provável</span>
    </div>

    <div class="answers">
        <a href="{{link}}/1?u={{userId}}">1</a>
        <a href="{{link}}/2?u={{userId}}">2</a>
        <a href="{{link}}/3?u={{userId}}">3</a>
        <a href="{{link}}/4?u={{userId}}">4</a>
        <a href="{{link}}/5?u={{userId}}">5</a>
        <a href="{{link}}/6?u={{userId}}">6</a>
        <a href="{{link}}/7?u={{userId}}">7</a>
        <a href="{{link}}/8?u={{userId}}">8</a>
        <a href="{{link}}/9?u={{userId}}">9</a>
        <a href="{{link}}/10?u={{userId}}">10</a>
    </div>
    <br />
    <br />
    <strong>Sua opinião é muito importante para nós</strong>
    <h3>Equipe | <strong>NLW</strong></h3>
</div>